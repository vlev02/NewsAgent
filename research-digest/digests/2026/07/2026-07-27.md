---
date: 2026-07-27
searched_tiers: [tier_0, tier_1, tier_2, watchlist]
---

### 🔥 今日必看

今天没有必须马上看的内容。tier_0/1/2/watchlist 逐项检索后，本轮命中的候选要么已在过去 7 天的
digest 中收录，要么发布时间早于 24-48 小时窗口（详见下方"过滤掉的"与"本轮搜索记录"）。

### 📄 新论文

#### KV Cache 压缩/驱逐
（本轮 tier_0 十一个关键词逐一检索，未搜到过去 24-48 小时内的新论文；命中的候选均早于窗口
或已在此前 7 天收录，见"过滤掉的"。）

#### KV Cache 复用 / Position Re-indexing
（本轮未搜到新论文，Jet-Long (2607.07740, Dynamic Bifocal RoPE) 等候选发布于本月早些时候，
不在 24-48 小时窗口内，暂不收录，留待后续确认是否需要补录。）

#### Systems / Serving
（本轮无独立新论文；相关工程动态见下方"工程/生态动态"。）

#### Agentic Context 管理
（本轮 tier_2 五个关键词逐一检索，未搜到过去 24-48 小时内、且落回 context/KV cache 管理机制
本身的新论文；命中的候选（Agentic Context Management、AREX 等）均已在此前收录。）

### 🏗️ 工程/生态动态

- **SGLang**：最新正式 release 仍是 7/25 的 v0.5.16（UnifiedRadixTree 默认化、DSA 跨 CP-rank
  KV 分片、ReplaySSM 环形验证等），与昨日（2026-07-26）digest 判断一致，本轮未发现更新版本。
- **vLLM**：最新正式 release 仍是 7/25 的 v0.26.0（KV cache 分层卸载/object-store 二级 tier、
  KV 布局重构等），与昨日 digest 判断一致，本轮未发现该 release 之外的新增内容。
- **LMCache**：最新正式 release 仍是 7/22 的 v0.5.2（CacheBlend 兼容 vLLM Hybrid Memory
  Architecture、strided fused-K re-RoPE、新增多个 L2 存储后端等），本轮未见更新的正式版本；
  仅有 7/23 的 nightly 构建（CUDA 12.9/13.0 wheel），非正式 release，不单独计入。

### 🗑️ 过滤掉的

- watchlist 11 个具体项目（SnapKV/ChunkKV/H2O/CacheBlend/CacheClip/SideQuest/IntentKV/MiniPIC/
  MEPIC/CacheSlide/Attention Matching KV）本轮均无新论文或新版本，均为已收录的既有工作，按预期
  跳过。唯一新信号是 llama.cpp 仓库里一条请求实现 Attention Matching 的 issue（#20037），属于
  社区 wishlist 而非实际实现/release，不计入工程动态。
- tier_0 中"token pruning attention"/"token merging ViT LLM"关键词命中的一批纯视觉 Transformer
  token 剪枝/合并论文（LearnPruner、IWP、CubistMerge、MeToM、InfoMerge 等）——面向纯视觉推理
  加速，无 KV cache 复用/驱逐或 agentic context 管理交集，按 deprioritize 规则过滤，与前几轮
  判定一致。
- "attention sink"关键词本轮持续被幻觉检测/可解释性/机制分析方向的论文（SinkTrack、Structural
  Origin of Attention Sink 等）占据，与 KV cache 系统层面工作的相关性持续偏低。
- tier_2 命中的若干候选（TokenPilot、ACE、Mem-π、Decision-Aware Memory Cards、Self-Compacting
  Language Model Agents 等）发布时间均早于本轮 24-48 小时窗口，且多数已在此前 7 天内以类似
  主题收录过同类工作，本轮不重复补录，仅记录以备后续核查。

### 本轮搜索记录

tier_0（KV cache compression / eviction / reuse / prefix caching LLM / position re-indexing /
RoPE re-rotation / semantic chunk eviction / attention sink / StreamingLLM / token pruning
attention / token merging ViT LLM）、tier_1（PagedAttention / vLLM KV cache / LMCache /
disaggregated prefill-decode / KV cache offloading CPU GPU / long context serving LLM /
speculative decoding KV cache / SGLang）、tier_2（long horizon agent context window / agent
memory context management / ReAct context compression / tool use context eviction agent / deep
research agent context length）、watchlist（11 个项目逐一检索 + vLLM/LMCache/SGLang GitHub
releases 页面核对）均已分别检索，未合并成大查询。本轮是近几轮里信号最少的一次：绝大多数搜索
命中的论文要么已在过去 7 天内收录（如 C²KV、Kamera、LazyAttention、Still、UltraQuant、SAC、
HERALD、Plans Don't Persist、AREX 等），要么发布时间明显早于 24-48 小时窗口。未发现需要新增
关键词或调整 tier 分层的信号。
