---
date: 2026-07-29
searched_tiers: [tier_0, tier_1, tier_2, watchlist]
---

### 🔥 今日必看

今天没有必须马上看的内容，常规更新见下方链接。

### 📄 新论文

（本轮 tier_0/1/2 逐关键词检索 + watchlist 11 个项目逐一检索，均未发现过去 24-48 小时内
新挂出、且与 KV cache 算法/系统层面或 agentic context/KV cache 管理直接相关的新论文。命中的
候选要么早于窗口、要么已在此前 7 天窗口内收录过，详见下方"过滤掉的"。）

#### KV Cache 压缩/驱逐

（无新增，见下方"过滤掉的"。）

#### KV Cache 复用 / Position Re-indexing

（无新增，见下方"过滤掉的"。）

#### Systems / Serving

（无新增，见下方"过滤掉的"。）

#### Agentic Context 管理

（无新增，见下方"过滤掉的"。）

### 🏗️ 工程/生态动态

- **vLLM**：搜索结果显示最新正式 release 版本号在 v0.25.0（7/11）与此前几日记录的 v0.26.0
  （时间戳 7/25-7/27）之间存在冲突，判断为搜索引擎索引滞后/缓存导致的信息不一致，未能确认
  7/28 之后是否有真正的新 release；建议下一轮直接核对 GitHub releases 页面而非依赖网页搜索
  摘要。
- **LMCache**：搜索命中的最新标签仍是 7/22-7/23 的 `v0.5.2-cu129` / `operator-v0.5.1`
  区间，与 7/28 digest 记录的 `operator-v0.5.2`（7/28）不完全一致，同样疑似索引滞后，未发现
  确凿的新增能力更新。
- **SGLang**：搜索命中 v0.5.15（7/10）与此前记录的 v0.5.16（7/25）均出现，未发现更晚的新
  release；v0.5.16 中集成的 DSpark（置信度调度的半自回归投机解码）已在 7 月初的
  DSpark 论文（2607.05147）中出现，非本轮新信号。

### 🗑️ 过滤掉的

- tier_0 十一个关键词（KV cache compression/eviction/reuse、prefix caching、position
  re-indexing、RoPE re-rotation、semantic chunk eviction、attention sink、StreamingLLM、
  token pruning/merging）逐一检索，命中均为 6 月-7 月中旬的既有工作（如 MosaicKV、C²KV、
  JoLT、KVpop、Error Certificates for KV-Cache Eviction 等），均已在此前若干轮 digest 中收录，
  或明显早于 24-48 小时窗口，本轮不重复补录。
- "token pruning attention"/"token merging ViT LLM" 继续被纯视觉 Transformer/视频 LLM 加速
  工作占据（LearnPruner、AgilePruner、ASAP、CubistMerge 等），与 KV cache 复用/驱逐或 agentic
  context 管理无交集，按 deprioritize 规则过滤，与前几轮判定一致。
- "attention sink"关键词命中持续偏向机制可解释性/幻觉检测方向（Attention Sink 综述、
  Attention Sinks as Internal Signals for Hallucination Detection 等），与 KV cache 系统层面
  工作相关性偏低，按既往判定过滤。
- tier_1 八个关键词（PagedAttention、vLLM KV cache、LMCache、disaggregated prefill-decode、
  KV cache offloading CPU GPU、long context serving LLM、speculative decoding KV cache、
  SGLang）命中的候选（ScoutAttention、SPHERICAL KV、ReContext 等）均早于窗口或已收录。
  "ReContext: Recursive Evidence Replay as LLM Harness for Long-Context Reasoning"
  （2607.02509，7/2 提交）虽落在 long context serving 关键词下，但核心是训练-free 的证据
  重放提升长上下文利用率、不涉及 KV cache 压缩/驱逐/复用机制本身，按相关性规则过滤，不收录。
- tier_2 五个关键词命中的候选（Remember When It Matters、Multi-Head Recurrent Memory
  Agents、AgentSwing、LCM: Lossless Context Management 等）均早于窗口或此前已判定过滤；
  "HANDBOOK.md: A Benchmark for Long-Context Agentic Instruction Following"
  （2607.25398，7/28 提交）落在窗口内但属于 agentic 指令跟随能力 benchmark，非 context/KV
  cache 管理机制本身，按 deprioritize 规则（generic benchmark leaderboard）过滤。
- watchlist 十一个具体项目（SnapKV/ChunkKV/H2O/CacheBlend/CacheClip/SideQuest/IntentKV/
  MiniPIC/MEPIC/CacheSlide/Attention Matching）本轮均无新论文或新 release，均为已收录的既有
  工作，按预期跳过。

### 本轮搜索记录

tier_0（KV cache compression / eviction / reuse / prefix caching LLM / position re-indexing /
RoPE re-rotation / semantic chunk eviction / attention sink / StreamingLLM / token pruning
attention / token merging ViT LLM）、tier_1（PagedAttention / vLLM KV cache / LMCache /
disaggregated prefill-decode / KV cache offloading CPU GPU / long context serving LLM /
speculative decoding KV cache / SGLang）、tier_2（long horizon agent context window / agent
memory context management / ReAct context compression / tool use context eviction agent / deep
research agent context length）、watchlist（11 个项目逐一检索 + vLLM/LMCache/SGLang GitHub
releases 页面核对）均已分别检索，未合并成大查询。额外用 arxiv ID 前缀（2607.2x）做了一轮新增
列表扫描以补捉搜索引擎摘要可能漏掉的最新提交，未发现符合相关性标准的新增信号。未发现需要新增
关键词或调整 tier 分层的信号；vLLM/LMCache 的 release 版本号信息本轮出现搜索结果内部不一致，
已在"工程/生态动态"中记录，供人工复核。
