---
date: 2026-07-30
searched_tiers: [tier_0, tier_1, tier_2, watchlist]
---

### 🔥 今日必看

今天没有必须马上看的内容，常规更新见下方链接。

### 📄 新论文

（本轮 tier_0/1/2 逐关键词检索 + watchlist 11 个项目逐一检索，均未发现过去 24-48 小时内
新挂出、且与 KV cache 算法/系统层面或 agentic context/KV cache 管理直接相关的新论文。命中的
候选要么早于窗口、要么已在此前 7 天窗口内收录过，详见下方"过滤掉的"。）

#### KV Cache 压缩/驱逐

（无新增，见下方"过滤掉的"。）

#### KV Cache 复用 / Position Re-indexing

（无新增，见下方"过滤掉的"。）

#### Systems / Serving

（无新增，见下方"过滤掉的"。）

#### Agentic Context 管理

（无新增，见下方"过滤掉的"。）

### 🏗️ 工程/生态动态

- **vLLM**：直接核对 releases 页面确认了此前几轮记录的日期冲突——完整时间线为 v0.25.0（7/11）
  →v0.25.1（7/14）→v0.26.0（7/27），此前"7/25 vs 7/27"的冲突是页面刷新/索引时间戳差异，非
  两次独立 release。v0.26.0 起 Model Runner V2 取代 PagedAttention 作为默认路径，并加入
  DeepSeek-V4 相关优化；截至本轮，v0.26.0（7/27）仍是最新版，7/27 之后未见新 release。
- **LMCache**：主库最新稳定版仍是 v0.5.2（7/22）。独立的 LMCache Operator 组件在 7/28 发布的
  `operator-v0.5.2` 经核对是纯文档性更新（CODEOWNERS 调整），无功能变化。另外注意到仓库存在
  7/30 的 nightly 预发布（CUDA 12.9/13.0 dev build），属持续滚动构建，非正式版本节点，不计入
  "新 release"。
- **SGLang**：最新正式 release 仍是 v0.5.16（7/25），集成的 DSpark 投机解码等能力已在此前几轮
  digest 中记录，本轮未发现更晚的新 release。

### 🗑️ 过滤掉的

- tier_0 十一个关键词逐一检索，命中集中在早于 24-48 小时窗口的既有工作：C²KV
  （2607.17715，~7/17，KV cache 压缩/复用方向强相关但已超出窗口）、vLLM 博客中关于 Kimi K3
  KDA-aware prefix caching 的介绍（~7/22，同样超出窗口）、KVpop（2607.05061）、STAR-KV
  （ICML spotlight 报道）、Error Certificates for KV-Cache Eviction（2607.21475）等，均已在
  此前 7 天窗口内的 digest 中收录或明显更早，本轮不重复补录。
- "attention sink"关键词命中持续被机制可解释性/幻觉检测方向占据（attention sink 综述、
  "how attention sinks emerge"等），与 KV cache 系统层面工作交集低，按既往判定过滤。
- "StreamingLLM"关键词命中仍以视频流理解/多模态实时交互工作为主（Video Streaming Thinking、
  OmniInteract 等），与 attention-sink 式 KV cache 管理脱钩，判定为无关。
- "token pruning attention"/"token merging ViT LLM"继续被纯视觉 Transformer/视频加速工作
  占据（LearnPruner、AgilePruner、CubistMerge 等），无 KV cache 复用/驱逐或 agentic context
  管理交集，按 deprioritize 规则过滤。
- tier_1 八个关键词命中的候选（DistrAttention、TurboQuant、KVarN、ScoutAttention 等）均早于
  窗口或已在此前收录，不重复补录。
- tier_2 五个关键词命中的候选（Self-GC、ACE、Parallel Context Compaction、AgentFold、
  Mem-π、Governance Decay 等）均早于窗口或已在此前 7 天窗口内收录过。
- watchlist 十一个具体项目逐一检索/核对（含对 adamzweiger/compaction、FasterDecoding/SnapKV
  的 GitHub 提交历史直接核查）：均无近期新论文或新 release，SnapKV 仓库自 2025-07 起无提交，
  compaction 仓库最后提交为 2026-03-31，均为已收录的既有工作，按预期跳过。特别记录：
  watchlist 中的 "MEPIC" 本轮未能找到对应的具体论文/项目/代码库，建议下一轮或人工核实这个
  条目的确切名称/来源，避免关键词库里存在无法追踪的死条目。

### 本轮搜索记录

tier_0（KV cache compression / eviction / reuse / prefix caching LLM / position re-indexing /
RoPE re-rotation / semantic chunk eviction / attention sink / StreamingLLM / token pruning
attention / token merging ViT LLM）、tier_1（PagedAttention / vLLM KV cache / LMCache /
disaggregated prefill-decode / KV cache offloading CPU GPU / long context serving LLM /
speculative decoding KV cache / SGLang）、tier_2（long horizon agent context window / agent
memory context management / ReAct context compression / tool use context eviction agent / deep
research agent context length）、watchlist（11 个项目逐一检索，其中 adamzweiger/compaction 与
FasterDecoding/SnapKV 额外直接核查 GitHub 提交历史）均已分别检索，未合并成大查询。vLLM/
LMCache/SGLang 三个 GitHub releases 页面均直接核对（非仅依赖搜索引擎摘要），解决了上一轮记录
的版本时间戳冲突。本轮判断是一个跨全部追踪方向的"安静窗口"——三个 serving 项目均恰好处于刚
发完一轮 release 之后（vLLM 7/27、SGLang 7/25、LMCache 7/22-28），下一波论文/release 尚未
落地。未发现需要新增关键词或调整 tier 分层的信号；watchlist 中 "MEPIC" 条目持续无法追踪到
具体来源，已在"过滤掉的"中记录供人工复核。
