---
date: 2026-08-22
searched_tiers: [tier_0, tier_1, tier_2, watchlist]
---

### 🔥 今日必看

（本轮 tier_0/1/2/watchlist 检索均未发现过去 24-48 小时内的新增相关信号，无。）

### 📄 新论文

#### KV Cache 压缩/驱逐

（tier_0 逐词检索：KV cache compression / eviction / semantic chunk eviction /
attention sink / StreamingLLM 均未发现过去 24-48 小时内新挂出的相关论文；命中的
均为已收录（AnchorKV 2608.02901、KV Cache Compression Through the Lens of
Transform Coding 2608.14191 等）或早于窗口的背景工作。）

#### KV Cache 复用 / Position Re-indexing

（tier_0 逐词检索：KV cache reuse / prefix caching LLM / position re-indexing /
RoPE re-rotation 均未发现新增,仅命中已收录的 ReCache（2608.19662,8/20,已于
8/21 轮次收录）与既有背景工作（SemPIC、Kamera 等）。）

#### Systems / Serving

（tier_1 逐词检索：PagedAttention/vLLM KV cache/LMCache/disaggregated
prefill-decode/KV cache offloading CPU-GPU/long context serving/speculative
decoding+KV cache/SGLang 均未发现过去 24-48 小时内的 KV cache 机制层面新论文或
新 release,与上一轮（8/21）核实结论一致。）

#### Agentic Context 管理

（tier_2 逐词检索：long horizon agent context window/agent memory context
management/tool use context eviction/multi-turn agent trajectory KV cache/
agentic context engineering system/prefix cache invalidation tool call/
shared prefix cache multi-agent/agentic workload inference profiling 均未发现
新增,命中的均为已收录（CommitKV 2608.07855、ReCache 2608.19662）或早于窗口的
背景工作（IntentKV、MemDecay、Leyline、TokenDance 等）。）

### 🏗️ 工程/生态动态

- **vLLM**：8/11 新增量化 DSpark Markov head 支持,7/27 发布 v0.26.0（Inkling
  模型支持、DeepSeek-V4 性能优化、fp32 生成头、灵活 attention backend、成熟的
  分层 KV offloading）,均非本轮 24-48 小时窗口内的新变更,与上一轮核实结论一致。
- **LMCache**：仍为此前核实的 v0.5.2（operator-v0.5.2,7/28）,本轮未发现更新的
  正式版本号；lmcache-vllm 子仓库有 v0.6.2.3 预发布（async store 修复、Turing
  GPU 支持等）,非核心 LMCache 主仓库版本。
- **SGLang**：仅发现 NVIDIA SGLang 容器 26.07（8月）release notes,内容为
  GB300/B300、RTX PRO 6000 Blackwell、DGX Spark、Jetson Thor 等硬件平台支持,
  非 KV cache 机制层面更新,与上一轮一致。
- watchlist 逐一核实：SnapKV/ChunkKV/H2O/CacheBlend/CacheClip/SideQuest/
  IntentKV/MiniPIC/MEPIC/CacheSlide/Attention Matching KV/Parrot/Preble/
  Autellix/InferCept 均未发现超出已知版本的新论文/release。

### 🗑️ 过滤掉的

- tier_0「token pruning attention」命中的新论文（如 Learning to Predict
  Middle-Layer Attention in MLLMs for Visual Token Pruning, 2608.06411,及
  LearnPruner 2604.23950）聚焦视觉/多模态 token 剪枝,不落在文本 LLM 的 KV
  cache 压缩/复用/驱逐机制上,按 deprioritize 规则过滤。
- tier_2「agentic workload inference profiling」命中的 CPU-Centric Perspective
  综述（2511.00739）与 ProfilingAgent（2509.05584）均聚焦模型优化/硬件层
  profiling,未触及 KV/cache 层面机制,过滤。
- tier_1「disaggregated prefill decode inference」命中的多篇论文（SLO-Aware
  Compute Resource Allocation、Beyond the Buzz 等）发布时间均早于本轮 24-48
  小时窗口且已被此前轮次判定为背景工作,未重复收录。

今日无新增相关论文/release。已按 keywords.yaml 完整遍历 tier_0（KV cache
compression/eviction/reuse/prefix caching LLM/position re-indexing/RoPE
re-rotation/semantic chunk eviction/attention sink/StreamingLLM/token pruning
attention）、tier_1（PagedAttention/vLLM KV cache/LMCache/disaggregated
prefill-decode/KV cache offloading CPU-GPU/long context serving/speculative
decoding+KV cache/SGLang）、tier_2（long horizon agent context window/agent
memory context management/tool use context eviction/multi-turn agent
trajectory KV cache/agentic context engineering system/prefix cache
invalidation tool call/shared prefix cache multi-agent/agentic workload
inference profiling）、watchlist（具名项目 + vLLM/LMCache GitHub release）
各关键词的独立检索。
