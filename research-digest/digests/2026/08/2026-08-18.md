---
date: 2026-08-18
searched_tiers: [tier_0, tier_1, tier_2, watchlist]
---

### 🔥 今日必看

（今天没有必须马上看的内容，常规更新见下方链接）

### 📄 新论文

#### KV Cache 压缩/驱逐

（无新增）

#### KV Cache 复用 / Position Re-indexing

（无新增）

#### Systems / Serving

（无新增）

#### Agentic Context 管理

（无新增）

#### Agent Runtime × LLM Runtime 协同

（无新增）

### 🏗️ 工程/生态动态

- **vLLM**：仍为 `v0.27.1`（8/11 挂出），未见新版本发布。
- **LMCache**：仍在 `v0.5.4rc4`（8/15 挂出）；今天（8/18）只新增了 CUDA 12.9/13.0 的
  nightly 构建（`dev` 分支），未见正式新 release note。
- **SGLang**：最新仍为 `v0.5.17`（8/8 挂出），未发现新版本。
- watchlist 逐一核实：SnapKV/ChunkKV/H2O/CacheBlend/CacheClip/SideQuest/IntentKV/
  MiniPIC/MEPIC/CacheSlide/Attention Matching KV/Parrot/Preble/Autellix/InferCept
  均未发现超出已知版本的新论文/release。

### 🗑️ 过滤掉的

- 本轮 tier_0/1/2/watchlist 四路独立检索（共约 60 余次分关键词搜索）均未命中
  2026-08-16 至 2026-08-18 这个新鲜度窗口内的新内容；检索到的最新候选普遍停留在
  8/1-8/8（如 AnchorKV 2608.02901、QEvict 2608.05326、MAP 2608.06411、
  WorldTrace 2608.07408 等），且均已在 `papers.jsonl` 中收录或早于本次 7 天排重
  窗口，判断为搜索工具对 arXiv 最近 1-2 天的索引滞后，而非真的没有新论文——
  下一轮运行时应重点回补 8/16-8/18 这个窗口。
- **Trie Automata for Constrained Decoding over Large Finite Sets**（arXiv
  2608.12574）— vLLM 场景下的语法约束解码（guided decoding）token mask 加速，
  不涉及 KV cache 机制本身；按 deprioritize 规则过滤。
- **Alignment Drift in Single-Model Speculative Decoding for ASR**（arXiv
  2608.12703）— 领域是语音识别（ASR）投机解码，非 LLM serving/KV cache；过滤。
- **Which Question Is Your Attention Metric Answering?**（arXiv 2608.14712）—
  attention 可解释性度量研究，不涉及 KV cache/serving 层；过滤。
- **Proof-Valid Caching under Premise Erasures**（arXiv 2608.11782）—
  信息论/证明缓存，与 transformer KV cache 无关（同名不同物）；过滤。
- tier_2 检索中命中的多篇纯文本 agent memory/context 管理工作（如 Structured
  Context Eviction 2606.11213、Self-GC 2607.00692、"Are We Ready For An
  Agent-Native Memory System?" 2606.24775 等）未落到 KV cache 存储/复用/驱逐
  机制层面，按 tier_2 判据过滤，不逐条列出。
- 若干长程 agent 能力评测榜单（WildClawBench、HANDBOOK.md、
  Long-Horizon-Terminal-Bench 等）未分析 context/KV 代价，按 deprioritize
  规则过滤。

搜索过的关键词：

- **tier_0**：KV cache compression / KV cache eviction / KV cache reuse / prefix
  caching LLM / position re-indexing KV cache / RoPE re-rotation cache / semantic
  chunk eviction / attention sink / StreamingLLM / token pruning attention /
  token merging ViT LLM
- **tier_1**：PagedAttention / vLLM KV cache / LMCache / disaggregated prefill
  decode inference / KV cache offloading CPU GPU / long context serving LLM /
  speculative decoding KV cache / SGLang
- **tier_2**：long horizon agent context window / agent memory context
  management / ReAct context compression / tool use context eviction agent /
  deep research agent context length / multi-turn agent trajectory KV cache /
  Plan-and-Execute agent context management / Reflexion agent context reuse /
  long horizon agent benchmark context length / agentic workload inference
  profiling / agentic context engineering system / structured agent memory KV
  cache / agent context offloading retrieval KV / prefix cache invalidation
  tool call / KV cache reuse multi-turn conversation / agent context growth
  inference cost / shared prefix cache multi-agent
- **watchlist**：SnapKV / ChunkKV / H2O heavy hitter oracle / CacheBlend /
  CacheClip / SideQuest / IntentKV / MiniPIC / MEPIC / CacheSlide / Attention
  Matching KV / Parrot / Preble / Autellix / InferCept / vLLM GitHub releases /
  LMCache GitHub releases / SGLang GitHub releases
- **tier_3**（辅助）：YaRN RoPE scaling / position interpolation long context /
  RoPE extrapolation / state space model long context Mamba / RWKV long
  context KV alternative
- **tier_4**（辅助）：agent runtime LLM serving co-design / KV cache aware
  agent scheduling / stateful LLM serving session cache / prefix aware
  scheduling LLM serving / LLM serving agentic workload / inference engine
  tool call interception / semantic aware LLM application serving
