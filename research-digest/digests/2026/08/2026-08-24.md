---
date: 2026-08-24
searched_tiers: [tier_0, tier_1, tier_2, tier_3, tier_4, watchlist]
---

### 🔥 今日必看

（本轮 tier_0/1/2/3/4/watchlist 检索均未发现过去 24-48 小时内的新增相关信号，无。今天没有必须马上看的内容，常规更新见下方。）

### 📄 新论文

今天没有新增相关论文。

#### KV Cache 压缩/驱逐

（tier_0 逐词检索：KV cache compression / KV cache eviction / semantic chunk eviction /
attention sink / StreamingLLM / token pruning attention / token merging ViT LLM，
并额外针对 2608.22xxx-2608.24xxx 号段做了定向核查。命中的均为已收录（KV Cache
Compression Through the Lens of Transform Coding 2608.14191、CacheCraft
2608.14555 等）或早于窗口的背景工作（AnchorKV 2608.02901、Output-Aware
Rotation for INT2 2608.02691、RestoreKV 2608.01247 等）。「token pruning
attention」/「token merging ViT LLM」命中的仍以视觉/视频 token 剪枝为主，按
deprioritize 规则过滤。）

#### KV Cache 复用 / Position Re-indexing

（tier_0 逐词检索：KV cache reuse / prefix caching LLM / position re-indexing /
RoPE re-rotation cache 均未发现新增，仅命中已收录的 ReCache（2608.19662，8/20）
与既有背景工作（CoinRAG 2608.07458、Cross-Model KV Cache Transfer
2608.03893 等）。）

#### Systems / Serving

（tier_1 逐词检索：PagedAttention / vLLM KV cache / LMCache / disaggregated
prefill-decode / KV cache offloading CPU-GPU / long context serving /
speculative decoding+KV cache / SGLang 均未发现过去 24-48 小时内的 KV cache
机制层面新论文。vLLM/LMCache/SGLang 版本状态见下方「工程/生态动态」。）

#### Agentic Context 管理

（tier_2 逐词检索 17 个关键词：long horizon agent context window / agent
memory context management / ReAct context compression / tool use context
eviction agent / deep research agent context length / multi-turn agent
trajectory KV cache / Plan-and-Execute agent context management / Reflexion
agent context reuse / long horizon agent benchmark context length /
agentic workload inference profiling / agentic context engineering system /
structured agent memory KV cache / agent context offloading retrieval KV /
prefix cache invalidation tool call / KV cache reuse multi-turn conversation /
agent context growth inference cost / shared prefix cache multi-agent 均未
发现新增，命中的均为已收录（CommitKV 2608.07855、ReCache 2608.19662、
CacheScout 2608.14624）或早于窗口的背景工作（Practical Online KV Cache
Compaction for LLM Agents 2608.00902 等）。）

#### Agent Runtime × LLM Runtime 协同（tier_4）

（tier_4 逐词核实：agent runtime LLM serving co-design / KV cache aware agent
scheduling / stateful LLM serving session cache / prefix aware scheduling
LLM serving / LLM serving agentic workload / inference engine tool call
interception / semantic aware LLM application serving 均未发现窗口内新增，
命中的均为早于窗口的背景工作（TokenCake 2510.18586、Continuum 2511.02230、
TokTier 2607.29678 等），CacheScout（2608.14624）再次出现但已在排除列表内。）

### 🏗️ 工程/生态动态

- **vLLM**：稳定版仍为 v0.27.1（8/11 发布）。发现 **v0.28.0rc1**（8/20 打
  tag）与 **v0.28.0rc2**（8/21 打 tag）——均早于本轮 48 小时窗口且为
  release-candidate，搜索未能核实其是否包含 KV cache 相关改动，记录在案供
  下一轮跟进正式版发布时核实 changelog。
- **LMCache**：本轮检索到的最新 tag 信息不一致（7/28 的 v0.5.2 vs 此前
  digest 记录的 8/20 v0.5.4），未发现晚于窗口的新 release；建议下一轮直接
  核对 GitHub releases 页面澄清版本号。
- **SGLang**：最新 tag 版本 v0.5.16（7/25 发布，已超出窗口），本轮窗口内未
  发现新 release 或 KV cache 机制相关公告。
- watchlist 逐一核实：SnapKV / ChunkKV / H2O / CacheBlend / CacheClip /
  SideQuest / IntentKV / MiniPIC / MEPIC / CacheSlide / Attention Matching
  KV / Parrot / Preble / Autellix / InferCept 均未发现超出已知版本的新论文/
  release。
- tier_3（位置编码/外推）逐词核实：YaRN RoPE scaling / position
  interpolation / RoPE extrapolation / SSM Mamba long context / RWKV long
  context 均未发现与 KV cache 管理有实质关联的窗口内新增。

### 🗑️ 过滤掉的

- tier_2「long horizon agent benchmark context length」命中的
  "Long-Horizon-Terminal-Bench"（2607.08964）只是 agent 能力评测榜单，仅
  带过一句 $/token 成本，不触及 KV/cache 机制，按 deprioritize 规则过滤。
- tier_2「agentic context engineering system」/「structured agent memory
  KV cache」命中的 "Multi-Agent Memory from a Computer Architecture
  Perspective"（2603.10062，已收录）同类的立场/愿景论文 "Are We Ready For
  An Agent-Native Memory System?"（2606.24775）无具体 KV 机制，过滤。
- tier_2「ReAct context compression」/「agent memory context management」
  命中的 "Cognitive Memory in Large Language Models"（2504.02441）是纯
  文本记忆分类综述，不触及 KV cache，过滤。
- tier_0「token pruning attention」「token merging ViT LLM」命中的视觉/
  视频领域 token 剪枝论文不落在文本 LLM 的 KV cache 机制上，过滤。

今日无新增相关论文/release。已按 keywords.yaml 完整遍历 tier_0（11 词）、
tier_1（8 词 + vLLM/LMCache/SGLang 版本核查）、tier_2（17 词）、tier_3（5
词）、tier_4（7 词）、watchlist（15 个具名项目）各关键词的独立检索，分派到
4 路并行搜索完成，均未发现符合「KV cache 算法/系统层面」或「agentic 场景下
落回 context/KV cache 管理」判据的新增内容。
