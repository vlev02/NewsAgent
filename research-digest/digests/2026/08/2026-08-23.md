---
date: 2026-08-23
searched_tiers: [tier_0, tier_1, tier_2, watchlist]
---

### 🔥 今日必看

（本轮 tier_0/1/2/watchlist 检索均未发现过去 24-48 小时内的新增相关信号，无。）

### 📄 新论文

今天没有新增相关论文。

#### KV Cache 压缩/驱逐

（tier_0 逐词检索：KV cache compression / eviction / semantic chunk eviction /
attention sink / StreamingLLM / token pruning attention / token merging ViT LLM
均未发现过去 24-48 小时内新挂出的相关论文；命中的均为已收录（KV Cache
Compression Through the Lens of Transform Coding 2608.14191、CacheCraft
2608.14555 等）或早于窗口的背景工作（AnchorKV 2608.02901、CONF-KV
2605.24786、SinkTrack 2604.10027 等）。「token pruning attention」/「token
merging ViT LLM」两词命中的仍以视觉/多模态 token 剪枝为主，按 deprioritize
规则过滤。）

#### KV Cache 复用 / Position Re-indexing

（tier_0 逐词检索：KV cache reuse / prefix caching LLM / position re-indexing /
RoPE re-rotation 均未发现新增，仅命中已收录的 ReCache（2608.19662，8/20）与
既有背景工作（AgentKVShift、Kamera、HijackKV 等）。）

#### Systems / Serving

（tier_1 逐词检索：PagedAttention / vLLM KV cache / LMCache / disaggregated
prefill-decode / KV cache offloading CPU-GPU / long context serving /
speculative decoding+KV cache / SGLang 均未发现过去 24-48 小时内的 KV cache
机制层面新论文。vLLM/LMCache GitHub release 详情见下方「工程/生态动态」。）

#### Agentic Context 管理

（tier_2 逐词检索：long horizon agent context window / agent memory context
management / ReAct context compression / tool use context eviction /
multi-turn agent trajectory KV cache / agentic context engineering system /
prefix cache invalidation tool call / shared prefix cache multi-agent /
agentic workload inference profiling 等 17 个关键词均未发现新增，命中的均为
已收录（CommitKV 2608.07855、ReCache 2608.19662、CacheScout 2608.14624）或
早于窗口的背景工作（MemDecay、Leyline、IntentKV、TokenPilot 等）。)

### 🏗️ 工程/生态动态

- **LMCache**：稳定版更新至 **v0.5.4**（"fleet-scale MP coordinator and
  broader hardware surface"），8/20 发布，比本轮 48 小时窗口早约 1 天，记录
  在案供下一轮核实细节；8/23 另有 CUDA 13.0/12.9、ROCm 7.2 的 nightly wheel
  构建标签，属例行自动化构建，不计入本轮更新。
- **vLLM**：最新版本仍为 v0.27.1（8/11 发布，quantized DSpark Markov head
  支持），本轮窗口内未发现新 release。
- **SGLang**：仅发现 NVIDIA SGLang 容器 26.07 例行刷新，非 KV cache 机制层面
  更新。
- watchlist 逐一核实：SnapKV / ChunkKV / H2O / CacheBlend / CacheClip /
  SideQuest / IntentKV / MiniPIC / MEPIC / CacheSlide / Attention Matching
  KV / Parrot / Preble / Autellix / InferCept 均未发现超出已知版本的新论文/
  release。
- tier_4（agent runtime ↔ LLM serving co-design）逐词核实：agent runtime LLM
  serving co-design / KV cache aware agent scheduling / stateful LLM serving
  session cache / prefix aware scheduling / LLM serving agentic workload /
  inference engine tool call interception / semantic aware LLM application
  serving 均未发现窗口内新增；LaCache（2608.01718，semantic aware serving，
  8/3 挂出）与 CacheScout（2608.14624，已收录）是最接近的历史命中。
- tier_3（位置编码/外推）逐词核实：YaRN RoPE scaling / position
  interpolation / RoPE extrapolation / SSM Mamba long context / RWKV long
  context 均未发现与 KV cache 管理有实质关联的窗口内新增。

### 🗑️ 过滤掉的

- tier_0「token pruning attention」「token merging ViT LLM」命中的新论文
  （视觉/视频领域 token 剪枝、CubistMerge、MeToM 等）不落在文本 LLM 的 KV
  cache 压缩/复用/驱逐机制上，按 deprioritize 规则过滤。
- tier_2「agentic context engineering system」命中的 "Context Engineering
  2.0"（2510.26493）为通用框架/立场综述，KV cache 仅作背景提及而非新系统
  贡献，过滤。
- tier_2「long horizon agent context window」命中的 "LLM Agents Are Latent
  Context Managers"（2606.30005）为纯 prompt 层的自我上下文管理仪表盘，不
  触及 KV 机制，过滤。
- tier_1「vLLM KV cache」命中的一篇 Medium 博客（"Fixing a KV Cache Bug in
  vLLM"，描述 OffloadingConnector 与逐 token-head 量化 KV cache 结合时的
  stride/layout 冲突）内容与主题相关，但确切发布日期无法核实是否落在本轮
  24-48 小时窗口内，且非 arXiv 论文/正式 release，本轮不计入正文，留待后续
  轮次如能确认日期再收录。

今日无新增相关论文/release。已按 keywords.yaml 完整遍历 tier_0（11 词）、
tier_1（8 词 + vLLM/LMCache GitHub release）、tier_2（17 词）、tier_3（5
词）、tier_4（7 词）、watchlist（15 个具名项目）各关键词的独立检索，均分派
到 4 路并行搜索完成，未发现符合 KV cache 算法/系统层面或 agentic 场景下
落回 context/KV cache 管理判据的新增内容。
