---
date: 2026-08-16
searched_tiers: [tier_0, tier_1, tier_2, watchlist]
---

### 🔥 今日必看

（今天没有必须马上看的内容，常规更新见下方链接）

### 📄 新论文

（今天没有新增相关论文——tier_0/1/2/watchlist 逐词检索命中的候选，逐一核对后均已在
`papers.jsonl` 中（`date_seen` 落在过去 7 天内，如 2608.12331/2608.11231/2607.28069/
2608.02515/2607.23809/2607.19214），或是发表日期明显早于 24-48 小时新鲜度窗口的背景
工作（如 AnchorKV 2608.02901、Cross-Model KV Cache Transfer 2608.03893、CoinRAG
2608.07458、SideQuest 2602.22603、IntentKV 2606.09916、MiniPIC 2606.13126、MEPIC
2512.16822、CacheSlide FAST'26、Attention Matching 2602.16284 等），没有发现落在新鲜度
窗口内且尚未收录的新论文。）

搜索过的关键词：

- **tier_0**：KV cache compression / KV cache eviction / KV cache reuse / prefix
  caching LLM / position re-indexing KV cache / RoPE re-rotation cache / semantic
  chunk eviction / attention sink / StreamingLLM / token pruning attention /
  token merging ViT LLM
- **tier_1**：PagedAttention / vLLM KV cache / LMCache / disaggregated prefill
  decode inference / KV cache offloading CPU GPU / long context serving LLM /
  speculative decoding KV cache / SGLang
- **tier_2**：agent memory context management / long horizon agent context window /
  tool use context eviction agent / deep research agent context length /
  multi-turn agent trajectory KV cache / agentic workload inference profiling /
  prefix cache invalidation tool call / agent context offloading retrieval KV /
  shared prefix cache multi-agent / KV cache aware agent scheduling / stateful
  LLM serving session cache / agent runtime LLM serving co-design
- **watchlist**：SnapKV / ChunkKV / H2O heavy hitter oracle / CacheBlend /
  CacheClip / SideQuest / IntentKV / MiniPIC / MEPIC / CacheSlide / Attention
  Matching KV / Parrot / Preble / Autellix / InferCept

#### KV Cache 压缩/驱逐

（无新增）

#### KV Cache 复用 / Position Re-indexing

（无新增）

#### Systems / Serving

（无新增）

#### Agentic Context 管理

（无新增）

### 🏗️ 工程/生态动态

- **vLLM**：仍为 `v0.27.1`（8/11 挂出），未见新版本；核实其 changelog 中 KV 相关条目
  （通用 P2P 二级 tier peer lookup/serving、TieringOffloadingSpec 的自描述 KV
  events、CPUOffloadingSpec 单份 MLA layout、per-layer canonical KV page mapping）
  已是此前观察窗口内容，无新增。
- **LMCache**：仍在 `v0.5.4` release candidate 系列迭代（8/15 挂出
  `v0.5.4rc3`/`v0.5.4rc4`），今天（8/16）只新增 CUDA 12.9/13.0 的 nightly 构建，未见
  正式 `v0.5.4` 稳定版发布。
- **SGLang**：最新仍为 `v0.5.17`（8/8 挂出，含 session-reference-aware Unified
  Radix Cache、HiCache L2 支持），未发现新版本。
- watchlist 逐一核实：SnapKV/ChunkKV/H2O/CacheBlend/CacheClip/SideQuest/IntentKV/
  MiniPIC/MEPIC/CacheSlide/Attention Matching KV/Parrot/Preble/Autellix/InferCept
  均未发现超出已知版本的新论文/release。

### 🗑️ 过滤掉的

- 本轮检索命中的候选绝大多数是已收录论文的重复命中，或发表时间明显落在新鲜度窗口
  之外的背景工作，未作为"过滤掉"单独记录；无与 tier_0/1/2 无交集但需要特别标注的
  噪音样本。
