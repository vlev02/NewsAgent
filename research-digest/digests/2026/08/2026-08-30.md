---
date: 2026-08-30
searched_tiers: [tier_0, tier_1, tier_2, watchlist]
---

### 🔥 今日必看

今天没有必须马上看的内容，常规更新见下方链接。

### 📄 新论文（按 tier 分类，每条一行：标题 + 一句话 takeaway）

#### KV Cache 压缩/驱逐

（tier_0 中"KV cache compression"/"KV cache eviction"/"attention sink"/"token
pruning attention"/"token merging ViT LLM"逐一检索，24-48 小时窗口内未发现
新的强相关命中。检索到的候选（如 AnchorKV 2608.02901、S⁴R 2608.00533）均已
超出窗口，且非本轮 papers.jsonl 缺失项。）

#### KV Cache 复用 / Position Re-indexing

（tier_0 中"KV cache reuse"/"prefix caching LLM"/"position re-indexing KV
cache"/"RoPE re-rotation cache"/"semantic chunk eviction"/"StreamingLLM"
逐一检索，本轮窗口内未发现新命中；候选（RestoreKV 2608.01247、Cross-Model
KV Cache Transfer 2608.03893 等）均超出窗口。）

#### Systems / Serving

（tier_1 八个关键词——PagedAttention / vLLM KV cache / LMCache / disaggregated
prefill decode inference / KV cache offloading CPU GPU / long context serving
LLM / speculative decoding KV cache / SGLang——逐一检索，未发现窗口内新
arXiv 论文命中。系统层面本轮无新信号，见下方"工程/生态动态"。）

#### Agentic Context 管理

（tier_2 十七个关键词逐一检索，本轮未发现同时满足"agentic"与"落到
KV/cache 层"两个条件的新论文。多数命中要么是纯文本层 context
管理/compaction（如 Context as an Environment 2608.21690、Toward Reliable
Context Compression 2608.06503），按 deprioritize 规则过滤；要么是已知的
窗口外背景工作（IntentKV 2606.09916、CacheScout 2608.14624 等，均已收录
或早于窗口）。）

### 🏗️ 工程/生态动态

- **vLLM**：仍为 **v0.28.0**（8/26），未发现新版本，与 8/29 digest 结论
  一致。
- **LMCache**：GitHub 上最新可见 tag 为 **operator-v0.5.4**（8/28），核实后
  该 tag 属于独立的 operator 发布轨道（k8s operator 组件），内容仍是给
  `lmcache bench engine` 增加 `--no-warmup` 参数，与此前几轮已记录的
  v0.5.5rc1 改动同源，非实质性更新。
- **SGLang**：仍为 v0.5.18（约 8/22），未发现新版本。
- watchlist 15 个具名项目（SnapKV / ChunkKV / H2O / CacheBlend / CacheClip /
  SideQuest / IntentKV / MiniPIC / MEPIC / CacheSlide / Attention Matching
  KV / Parrot / Preble / Autellix / InferCept）逐一核实，均未发现新版本/
  新论文。**MiniPIC 命名澄清**：此前几轮怀疑是笔误，本轮核实到确有同名
  项目 **MiniPIC: Flexible Position-Independent Caching in <100LOC**（arXiv
  2606.13126，6 月挂出，vLLM 上的极简 position-independent caching 实现，
  与 EPIC/Block-Attention/Prompt Cache 同源），此前判断有误，予以更正；
  该论文本身超出本轮窗口，不作为新收录。
  venues_and_sources（arXiv cs.CL/cs.LG/cs.DC、MLSys、OSDI/SOSP、
  NeurIPS/ICLR systems track）逐一检索，未发现窗口内新命中，与 8/29 一致
  （MLSys'26、OSDI'26 均已开完，进入淡季）。

### 🗑️ 过滤掉的

- **Context as an Environment: Programmatic Context Management for
  Long-Horizon Agents**（arXiv 2608.21690，~8/21）— 用可执行 Python
  kernel + append-only Event Log 做 agent session 的程序化上下文管理，
  是纯文本层 context 操作（代码搜索/变换 session state），未触及 KV
  cache 机制，按 deprioritize 规则过滤；且已超出 24-48 小时窗口。
- **Toward Reliable Context Compression for Long-Horizon Agents: An
  Empirical Study of Execution Instability**（arXiv 2608.06503，~8/6）—
  研究 context compression 对 agent 执行稳定性的影响，属于 compression
  策略的实证评测而非新方法，且不落到 KV cache 层，过滤；同时超出窗口。
- **Active Inference as Context Acquisition for AI Agents**（arXiv
  2608.19202，~8/19）— 用 active inference 框架做 agent 的上下文获取
  决策，纯认知框架/prompt 层工作，不涉及 KV cache，过滤；超出窗口。

### 📝 检索范围记录

tier_0（11 词）、tier_1（8 词，含 vLLM/LMCache/SGLang 版本核查）、tier_2
（17 词）、watchlist（15 个具名项目 + arXiv cs.CL/cs.LG/cs.DC 与
MLSys/OSDI/SOSP/NeurIPS/ICLR systems track venue 检索）均已逐一分别检索，
未合并成大查询。另追加两轮针对"过去 24-48 小时"的直接时间窗检索
（"KV cache arxiv August 28/29/30 2026"等）作为交叉验证，同样未发现新
命中。

**结论**：本轮为信号淡季——搜到的候选要么已在此前批次收录（如 TwinKV
2608.27128、Affix Cache 2608.26140、TOPAS 2608.25523），要么超出 24-48
小时窗口，要么不满足 KV cache/context 层相关性判据。vLLM/LMCache/SGLang
均无新版本。本轮未新增任何论文进 `papers.jsonl`。
