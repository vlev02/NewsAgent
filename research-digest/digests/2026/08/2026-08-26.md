---
date: 2026-08-26
searched_tiers: [tier_0, tier_1, tier_2, watchlist]
---

### 🔥 今日必看

今天没有必须马上看的内容，常规更新见下方链接。

### 📄 新论文

tier_0/1/2/watchlist 逐一分别检索（未合并成大查询），过去 24-48 小时窗口内未发现
真正新增、且落在 KV cache 算法/系统层或 agentic-KV 交叉点上的论文。命中的条目要么是
`papers.jsonl` 过去 7 天内已收录的老熟人（WnW/2608.22704、ReCache/2608.19662、
CommitKV/2608.07855、CacheScout/2608.14624、CacheCraft/2608.14555 等再次被索引到），
要么是发布日期更早、已超出"过去 24-48 小时"窗口且早于此前几轮补录范围的旧论文，
未见可补录的遗漏项。

#### KV Cache 压缩/驱逐
（本轮无新增）

#### KV Cache 复用 / Position Re-indexing
（本轮无新增）

#### Systems / Serving
（本轮无新增）

#### Agentic Context 管理
（本轮无新增；tier_2 17 个关键词逐一检索，命中均为纯文本层 agent 记忆/上下文管理框架
或纯能力评测，未触及 KV/cache 层，按 deprioritize 规则过滤，见下方"过滤掉的"。）

### 🏗️ 工程/生态动态

- **vLLM**：搜索引擎索引显示已出现 **v0.28.0** 正式版（8/24），号称包含 Kimi-K3/
  DeepSeek-V4 性能优化、投机解码与 Model Runner V2 演进、新 Rust frontend/gRPC，以及
  **tiered KV cache offloading**（GPU HBM → CPU DRAM → NVMe SSD 三层、对象存储 tier
  经 NIXL OBJ backend）。该特征与此前记录的 v0.28.0rc1/rc2（8/20-8/21，仍为
  release-candidate）在版本号上衔接，如确认属实即为 rc→正式版的转正。但本轮受
  egress 限制无法直接访问 `github.com/vllm-project/vllm/releases` 或
  `blog.vllm.ai` 核实原文，且另有一条搜索结果仍将 v0.27.0（8/10）报告为"最新"，
  两条结果相互矛盾，故本条标记为**未核实**，留待下一轮以能访问 GitHub 的检索方式复核。
- **SGLang**：搜索引擎索引结果同样滞后不一致（部分结果仍停留在 v0.5.17/8-8），与
  昨日已确认的 v0.5.18（8/22 打 tag）不矛盾但未提供更新信息，本轮未发现晚于 v0.5.18
  的正式 release。
- **LMCache**：搜索引擎索引仍主要返回 v0.5.2（7/22），未见晚于昨日已确认的 v0.5.4
  （8/20）的新 release。
- watchlist 逐一核实：SnapKV / ChunkKV / H2O / CacheBlend / CacheClip / SideQuest /
  IntentKV / MiniPIC / MEPIC / CacheSlide / Attention Matching KV / Parrot / Preble /
  Autellix / InferCept 均未发现超出已知版本的新论文/release。

### 🗑️ 过滤掉的

- **FreeToken: Efficient Edge-Native MoE Serving with Bandwidth-Adaptive Execution**
  （arXiv 2608.16157，约 8/16 挂出）— 维护"semantic-aware state cache"，但核心贡献是
  边缘 MoE 带宽自适应执行调度，未见对 KV cache 压缩/驱逐/复用机制的直接贡献；且已超出
  24-48 小时窗口，过滤。
- IBM/Red Hat 关于 P2P KV cache sharing 的技术博客（约 8/15 发布）— 内容与已知的
  prefix cache 复用/负载均衡主题相关，但已超出本轮时间窗口且未见明显超出已收录
  Preble/CacheScout 等工作的新信息，过滤（非本轮新增）。
- tier_0「token pruning attention」「token merging ViT LLM」命中的仍以视觉/视频领域
  token 剪枝为主（SlimInfer、LearnPruner 等），不落在文本 LLM 的 KV cache 机制上，过滤。
- tier_2「long horizon agent context window」「agent memory context management」命中的
  ACE、Self-GC、PEEK 等长程 agent 上下文管理框架论文均为已知或纯 prompt/文本层工作，
  未见新增且不触及 KV 存储/淘汰机制，过滤。
- tier_4「agent runtime LLM serving co-design」命中的 PRISM、A Policy-Driven Runtime
  Layer for Agentic LLM Serving 等均为已知或发布日期更早的工作，非本轮新增，过滤。

### 📝 检索范围记录

tier_0（11 词）、tier_1（8 词 + vLLM/LMCache/SGLang release 核查）、tier_2（17 词，
拆分为 agent memory/长程 agent/runtime co-design/prefix cache 工具调用等若干独立
查询）、watchlist（15 个具名项目逐一核实）均已逐一分别检索，未合并成大查询。

**本轮方法论限制**：与此前几轮一致，`arxiv.org`/`export.arxiv.org`/`blog.vllm.ai` 的
直接访问仍被环境出站代理拦截（`EGRESS_BLOCKED`），检索完全依赖 WebSearch 索引摘要；
本轮多次遇到搜索引擎索引滞后/相互矛盾的情况（尤见 vLLM 版本号），已在上文逐条标注
未核实状态，未将其计入"新论文"或"今日必看"。

本轮新收录论文：0 篇。`papers.jsonl` 本次无追加。
