---
date: 2026-09-02
searched_tiers: [tier_0, tier_1, tier_2, tier_3, tier_4, watchlist]
---

### 🔥 今日必看

（今天没有必须马上看的内容，常规更新见下方链接）

### 📄 新论文（按 tier 分类，每条一行：标题 + 一句话 takeaway）

#### KV Cache 压缩/驱逐

（tier_0 中"KV cache compression"/"KV cache eviction"/"attention sink"/"token pruning attention"/"token merging ViT LLM"逐一检索，24-48 小时窗口内未发现新的强相关命中；命中的候选（如 KVComp、The risk of KV cache compression、CapKV、VaSE 等）均为此前月份挂出、且方向上已被现有条目覆盖或与判据不直接相交。）

#### KV Cache 复用 / Position Re-indexing

（tier_0 中"KV cache reuse"/"prefix caching LLM"/"position re-indexing KV cache"/"RoPE re-rotation cache"/"semantic chunk eviction"/"StreamingLLM"逐一检索，本轮窗口内未发现新命中；检索到的 C²KV（2607.17715）、LinearKV（2608.11231）、HijackKV（2607.19957）等均为 7/8 月挂出的旧条目，方向上与已收录工作重叠。）

#### Systems / Serving

（tier_1 八个关键词——PagedAttention / vLLM KV cache / LMCache / disaggregated prefill decode inference / KV cache offloading CPU GPU / long context serving LLM / speculative decoding KV cache / SGLang——逐一检索，未发现窗口内新 arXiv 论文命中，检索结果集中在已被引用的 DistServe/CachedAttention 及此前已收录/超窗口的工作（如 HERALD、Tutti、ScoutAttention）。）

#### Agentic Context 管理

（tier_2 十七个关键词逐一检索，命中要么已收录（CommitKV 2608.07855、IntentKV 2606.09916、AgentKVShift 2607.21604、TOPAS 2608.25523、CacheScout 2608.14624），要么超出窗口且此前未收录（如 Context as an Environment 2608.21690、8/21 挂出；From LLM Inference to Agentic Workloads 2608.15127、8/15 挂出；ContextPilot 2608.28476）——经判断这几篇核心贡献落在纯 context/agent 编排框架或 workload 特征刻画，未直接触及 KV cache 存储/复用/驱逐机制或仅在讨论中提及，按 deprioritize 规则过滤，不强行收录以凑数量。）

#### Agent Runtime × LLM Runtime 协同（tier_4：prefix-aware 调度、stateful serving、tool-call 拦截）

（tier_4 七个关键词逐一检索，未发现窗口内新命中；A Policy-Driven Runtime Layer（2605.27744）、TokTier（2607.29678）、TVCACHE（2602.10986）均为此前月份挂出的旧条目。）

### 🏗️ 工程/生态动态

- **vLLM**：仍为 v0.28.0（8/24 发布），未发现新版本。
- **LMCache**：PyPI 上出现 `0.5.5rc3`（release candidate，9/2 上传），尚未发布正式 release，主线 GitHub releases 页面未见对应新版本，持续关注是否转正。
- **SGLang**：最近确认的正式 tag 仍为 v0.5.16（7/25），GitHub 上 9/2 有多个 PR 活跃合并但未见新版本公告。
- watchlist 15 个具名项目（SnapKV / ChunkKV / H2O / CacheBlend / CacheClip / SideQuest / IntentKV / MiniPIC / MEPIC / CacheSlide / Attention Matching KV / Parrot / Preble / Autellix / InferCept）逐一核实，均未发现新版本/新论文，检索结果均落在原始发布或此前已收录版本上。

### 🗑️ 过滤掉的

- **Context as an Environment: Programmatic Context Management for Long-Horizon Agents**（arXiv 2608.21690，8/21 挂出）— 提出 Scroll，用 append-only Event Log + 沙盒 Python kernel 做程序化上下文管理；核心贡献是把上下文管理变成编程任务，未落到 KV cache 存储/复用/驱逐等系统机制，按 deprioritize（agent 编排框架本身，无 serving/KV 层贡献）过滤。
- **From LLM Inference to Agentic Workloads: Characterization and Implications for Serving Systems**（arXiv 2608.15127，8/15 挂出）— 刻画 agentic workload 相对传统 LLM 推理的系统特征（非 LLM 组件延迟占比等），未见明确落到 KV cache 层的分析，暂判不相关；如后续版本补充 KV 层含义可重新评估。
- **ContextPilot: Teaching Agents for Proactive Context Management via Fine-grained RL**（arXiv 2608.28476，约 8/28 挂出）— 用细粒度 RL 训练 agent 主动做规划/长期记忆/软上下文卸载，核心是训练方法与工具集设计，未见触及 KV cache 存储/驱逐机制，按 deprioritize 过滤。

### 📝 检索范围记录

tier_0（11 词）、tier_1（8 词）、tier_2（17 词）、tier_3（5 词：YaRN/位置插值/RoPE 外推/Mamba/RWKV，背景性核查，未发现新强相关命中）、tier_4（7 词）、watchlist（15 个具名项目）均已逐一分别检索，未合并成大查询；另追加直接时间窗检索（"KV cache arxiv September 1/2 2026"）与工程生态（vLLM/SGLang/LMCache release）检索作为交叉验证。

**结论**：本轮未发现符合"KV cache 算法/系统层面"或"agentic 场景落回 KV cache 管理"判据、且不在最近 7 天已收录列表中的新论文，`papers.jsonl` 无新增。发现 3 条 tier_2 候选（2608.21690、2608.15127、2608.28476）判定核心贡献未触及 KV/cache 层，已过滤并记录理由。LMCache 出现 0.5.5rc3 release candidate，其余生态项目无新版本。连续两轮 tier_0/tier_1/tier_4 无新增信号，属正常波动，暂不调整 keywords.yaml。
